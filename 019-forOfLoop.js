/**
 * The "for...of" loop in javaScript is a modern ieration statement introduced in ECMAScript 6(ES6) that provides a concise and easy way to loop over elements in iterable directly over the values of the elements, rather than dealing with their indices or keys, which makes the code more readable and less error-prone.
 */

// let  fruits = [ "grapes","guava"];
// for(let fru of fruits){
//     console.log(fru);
// }

const text ="Ashok";
for(let char of text) {
    console.log(char);
}
